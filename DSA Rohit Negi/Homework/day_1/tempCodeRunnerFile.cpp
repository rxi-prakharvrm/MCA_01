tDigit);
        // (lastDigit == 11) ? lastDigit = 'B' : to_string(lastDigit);
        // (lastDigit == 12) ? lastDigit = 'C' : to_string(lastDigit);
        // (lastDigit == 13) ? lastDigit = 'D' : to_string(lastDigit);
        // (lastDigit == 14) ? lastDigit = 'E' : to_string(lastDigit);
        // (lastDigit == 15) ? lastDigit = 'F' : to_string(lastDigit);